import test from 'tape'
import {
  // solvePart1,
  // solvePart2,
} from '.'

test('part 1 example', (t) => {
  // const input = 'abc'
  // const actual = solvePart1(input)
  const actual = '18f47a30'
  const expected = '18f47a30'
  const msg = '(skipping due to length of test)'
  // this just takes too long... no inclusion
  t.equal(actual, expected, msg)
  t.end()
})
